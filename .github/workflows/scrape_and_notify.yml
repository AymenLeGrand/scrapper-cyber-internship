name: Scrape, Verify & Alert M2 Internships

on:
  schedule:
    # Automatically runs every 12 hours (08:00 & 20:00 UTC)
    - cron: '0 8,20 * * *'
  workflow_dispatch: # Allows you to manually trigger it anytime with 1 click

permissions:
  contents: write

jobs:
  scrape-and-notify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run 12-Hour Verification Pipeline & Mobile Alerts
        env:
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          python scripts/check_and_notify.py

      - name: Commit and Push Updated Data
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/jobs.json data/seen_jobs.json
          git diff --staged --quiet || git commit -m "Auto-refresh: Scraped & verified active M2 internships [skip ci]"
          git push
